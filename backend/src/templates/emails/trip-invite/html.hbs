<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trip Invitation - {{tripName}}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            padding: 30px 40px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 600;
        }
        .content {
            padding: 40px;
        }
        .greeting {
            font-size: 18px;
            margin-bottom: 20px;
            color: #444;
        }
        .button {
            display: inline-block;
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            text-decoration: none;
            padding: 15px 30px;
            border-radius: 6px;
            font-weight: 600;
            margin: 20px 0;
            text-align: center;
            box-shadow: 0 4px 12px rgba(40, 167, 69, 0.4);
        }
        .trip-details {
            background: #f8f9fa;
            border-left: 4px solid #28a745;
            padding: 20px;
            margin: 20px 0;
            border-radius: 4px;
        }
        .footer {
            background: #f8f9fa;
            padding: 30px 40px;
            text-align: center;
            border-top: 1px solid #dee2e6;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎒 Trip Invitation</h1>
        </div>
        <div class="content">
            <div class="greeting">
                Hi {{recipientName}}! ✈️
            </div>
            <p>{{inviterName}} has invited you to join an exciting trip!</p>

            <div class="trip-details">
                <h3>{{tripName}}</h3>
                <p><strong>Description:</strong> {{tripDescription}}</p>
                <p><strong>📅 Dates:</strong> {{tripDates}}</p>
                <p><strong>📍 Location:</strong> {{tripLocation}}</p>
                <p><strong>🔗 Invite Code:</strong> <code>{{inviteCode}}</code></p>
            </div>

            <div style="text-align: center;">
                <a href="{{inviteLink}}" class="button">Join This Trip</a>
            </div>
        </div>
        <div class="footer">
            <p>© {{year}} {{companyName}}. All rights reserved.</p>
        </div>
    </div>
</body>
</html>