# Frontend environment variables for API configuration

# API Base URL - Update this to point to your backend
VITE_API_BASE_URL=http://localhost:3000/api

# Environment (development, staging, production)
NODE_ENV=development

# Enable/disable debug logging in development
VITE_DEBUG_MODE=true

# Frontend application settings
VITE_APP_NAME=Group Planner
VITE_APP_VERSION=1.0.0

# Optional: Override token storage preferences
# VITE_STORE_TOKENS_IN_MEMORY_ONLY=false