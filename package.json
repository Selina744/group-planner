{
  "name": "group-planner",
  "version": "1.0.0",
  "description": "A collaborative trip planning application",
  "type": "module",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=20"
  },
  "packageManager": "bun@1.1.34",
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "test": "bun --filter '*' test",
    "test:setup": "bun --filter backend test:setup",
    "test:all": "bun --filter backend test:all && bun --filter frontend test",
    "test:teardown": "bun --filter backend test:teardown",
    "lint": "bun --filter '*' lint",
    "typecheck": "bun --filter '*' typecheck",
    "clean": "bun --filter '*' clean"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "keywords": ["trip-planning", "collaboration", "travel", "group-planning"],
  "author": "Group Planner Team",
  "license": "MIT"
}